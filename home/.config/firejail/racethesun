# Deny access to everything in $HOME except what it needs
# (Exempt it from anything generic.profile may do and whitelist it)
private ~/.local/share/RaceTheSun

# Turn on the standard set of protections I use for all games
include ${HOME}/.config/firejail/game-internet.inc
